#include<stdio.h>

// Bitwise operators are &(And); |(Or); ^(Xor)

int main()
{
    /* code */
    int a,b;
    a = 8;
    b = 6;

    printf("a&b=%d\n",a&b);
    printf("a|b=%d\n",a|b);
    printf("a^b=%d",a^b);
    
    return 0;
}
